# Frontend Environment Variables

# API Base URL (used in production)
# For development, the Vite proxy will handle API requests
# For Docker/production, set this to the backend URL
VITE_API_URL=http://localhost:8000
